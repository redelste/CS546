Readme:

First step, install all of the packages.
Second step, seed the database by running:
  npm run seed
Third step, start the server by running:
  npm start
Fourth step, open the site via localhost:3000
Fifth step, enjoy our site!
